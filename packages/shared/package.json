{
  "name": "@alankara/shared",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "lint": "echo 'No lint configured for shared'",
    "test": "echo 'No tests configured for shared'"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
