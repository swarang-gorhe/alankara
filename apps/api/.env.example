# Database
DATABASE_URL=postgresql://alankara:alankara_dev@localhost:5432/alankara
REDIS_URL=redis://localhost:6379/0

# Auth (Phase 6)
JWT_SECRET=change-me-in-production
JWT_ALGORITHM=HS256

# AI / LangChain (Phase 7)
LLM_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
ANTHROPIC_API_KEY=
ANTHROPIC_CHAT_MODEL=claude-3-5-haiku-latest

# Storage
STORAGE_BACKEND=local
LOCAL_UPLOAD_DIR=./uploads

# Supabase (Phase 8 — production)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_STORAGE_BUCKET=product-images

# Server
ENVIRONMENT=local
CORS_ORIGINS=http://localhost:3000

# Dev admin login (Phase 6 scaffold — change in production)
# POST /auth/login with admin@alankara.local / admin-dev-only
