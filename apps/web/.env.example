# API
NEXT_PUBLIC_API_URL=http://localhost:8000

# Site (SEO — canonical URLs, sitemap, Open Graph)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase (Phase 8 — production auth + optional direct reads)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Auth provider: jwt (dev default) | supabase (production)
NEXT_PUBLIC_AUTH_PROVIDER=jwt

# Stripe test publishable key (optional — checkout works in local test mode without it)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
